# دليل تشغيل مشروع KUN S2S Nexus على نظام Windows

تم تحديث المشروع ليدعم نظام تشغيل Windows بالكامل. فيما يلي الخطوات والتغييرات التي تم إجراؤها لضمان التوافق.

## 📋 المتطلبات المسبقة
1. **Python 3.10+**: تأكد من تثبيت Python وإضافته إلى مسار النظام (PATH).
2. **Windows Terminal**: يفضل استخدامه للحصول على أفضل تجربة مع واجهة TUI.

## 🛠️ التغييرات والميزات الجديدة
1. **إدارة العمليات (Process Management)**:
   - تم استبدال الكود الذي يعتمد على `/proc` (الخاص بـ Linux/Termux) بمكتبة `psutil`.
   - الآن يقوم النظام بالبحث عن أي نسخ أخرى من `main.py` وقتلها تلقائياً لمنع التعارض.

2. **التبعيات (Dependencies)**:
   - تمت إضافة `psutil` لإدارة العمليات.
   - تمت إضافة `windows-curses` (مشروط لنظام Windows) لضمان عمل الواجهات النصية بسلاسة.
   - تمت إضافة `fastapi`, `uvicorn`, `jinja2` لدعم واجهة الويب.

3. **واجهة الويب الاحترافية (New)**:
   - تم إضافة لوحة تحكم كاملة تعمل على المتصفح لإدارة المشروع.
   - الرابط الافتراضي: `http://localhost:8000`
   - الميزات: إحصائيات حية، إدارة الألعاب، قائمة المستخدمين، وسجلات مباشرة (Live Logs) عبر WebSockets.

## 🚀 كيفية التشغيل

1. **تثبيت المكتبات**:
   افتح موجه الأوامر (CMD أو PowerShell) في مجلد المشروع ونفذ:
   ```powershell
   pip install -r requirements.txt
   ```

2. **ضبط الإعدادات**:
   تأكد من وجود ملف `.env` يحتوي على التوكن الخاص بالبوت:
   ```env
   BOT_TOKEN=your_token_here
   ADMIN_ID=your_id_here
   ```

3. **تشغيل النظام**:
   ```powershell
   python main.py
   ```
   - ستظهر واجهة المدير (TUI) في التيرمنال.
   - سيبدأ خادم الويب تلقائياً في الخلفية.
   - يمكنك فتح المتصفح والذهاب إلى **http://localhost:8000** للوصول للوحة التحكم.

## ⚠️ ملاحظات هامة
- عند التشغيل لأول مرة، سيقوم النظام بإنشاء ملفات قاعدة البيانات `kun_nexus.db` و `nexus_jobs.db` محلياً.
- واجهة الويب تعمل على الشبكة المحلية (Localhost)، ويمكن الوصول إليها من أي جهاز على نفس الشبكة إذا قمت بفتح المنفذ 8000 في جدار الحماية (Firewall).
